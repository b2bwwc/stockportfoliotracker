<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <%= link_to "Team Portfolio Tracker", root_path, class: "navbar-brand" %>
    </div>

    <ul class="nav navbar-nav navbar-right" style="padding-top:5px; padding-right:5px;">
      <% if current_user && !(current_admin) %>
        <%= link_to "Home", user_path(current_user.id) %> |
        <%= link_to "Sign Out", destroy_user_session_path, method: :delete %>
      <% elsif !(current_user) && current_admin %>
        <%= link_to "Sign Out", destroy_admin_session_path, method: :delete %>
      <% else %>
        <%= link_to "User Sign In", new_user_session_path %> |
        <%= link_to "Admin Sign In", new_admin_session_path %>
      <% end %>
    </ul>
  </div>
</nav>
