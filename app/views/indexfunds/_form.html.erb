<%= form_for(@indexfund) do |f| %>
  <% if @indexfund.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@indexfund.errors.count, "error") %> prohibited this indexfund from being saved:</h2>

      <ul>
      <% @indexfund.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions input-group">
    <%= f.label :IndexFund, class:"input-group-addon" %>
    <%= f.text_field :name, class:'form-control' %>
  </div>
  
  <div class="actions input-group">
    <%= f.submit class:'btn btn-default' %>
  </div>
<% end %>
